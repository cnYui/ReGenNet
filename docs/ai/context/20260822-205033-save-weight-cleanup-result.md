# save 权重与 checkpoint 清理结果

## 执行结果

- 已删除 `save/**/*.pt` 共 2,579 个文件。
- 删除前约占 91.48GiB；当前 `save` 占用约 96MiB，释放约 91.38GiB。
- 复核确认 `save` 中剩余 `.pt` 文件数为 0。
- 剩余 824 个文件均为配置、指标、日志、PID、退出标记或 `.npy` 诊断数据，未删除。
- 未发现空目录，也未发现正在运行的训练进程。

## 影响

原有模型和优化器 checkpoint 已不可直接恢复；实验配置和结果记录仍可用于追溯。后续如需推理或继续训练，需要重新生成权重。
