# ReGenNet AI 入口

> **只修改 `AGENTS.md`。** `CLAUDE.md` 是指向本文件的软链接（Claude Code 读 `CLAUDE.md`，Codex 等读 `AGENTS.md`，内容是同一份）；不要编辑、删除或替换 `CLAUDE.md`。

## 稳定约束

- 默认使用中文；文档、计划、总结和代码注释均使用中文，除非用户明确要求英文。
- 实现前必须先完成 design / plan；把上下文、决策、取舍和结果写入 `docs/ai/context/`。
- 新增上下文只创建 `YYYYMMDD-HHMMSS-文件名.md`，不修改、不重命名历史文件；删除只由每日文档维护任务执行（`scripts/prune_ai_context.py`：文件名日期超过 15 天、且未被入口/仓库文件或其它保留文档引用、无 `<!-- prune:keep -->` 标记），需长期保留的文档要被入口引用或加该标记；被删文件可从 git 历史找回。
- 代码注释写原因，不写过程；优先函数式、复用现有模块，保持 KISS/DRY。
- `CLAUDE.md` 是指向 `AGENTS.md` 的软链接，只维护 `AGENTS.md`；CI 会拦截把它替换成普通文件的 PR。
- 代码提交：在 feature 分支提交后运行 `python3 scripts/ship_pr.py`（本机 Claude Code 审查 → 推送并向 `fork`（cnYui/ReGenNet）建 PR → CI 约定检查通过后自动 merge 并删除远端分支 → 本地 `main` 快进、删除已合并分支）；只同步本地用 `python3 scripts/ship_pr.py sync`。`origin` 为原作者仓库，不提交。本地 `main` 上游为 `fork/main`。

## 当前研究入口

- 已完成主线：InterHuman 双人动作预测，固定 `150/30/120` 协议；详细结果见 `docs/ai/context/20260614-185753-regennet-key-context-extracted-from-agents.md` 及其引用文档。
- 当前 NTU 双人 xyz 主线：必须使用真实双人 skeleton/xyz 口径，并将模型与 `copy-last` 同时评估；主指标为 paired `xyz_mse/xyz_mae/mpjpe` 等，动作分类、FID 和视频仅作辅助证据。
- 新增独立单人学习型 baseline：复用 `NTULabelXYZTransformer`，`num_persons=1` 且 A/B 参数共享；每个人只看自己的 obs10 与动作标签，分别预测 future50 后拼回双人。它与 `copy-last`、联合双人 direct reference 分开报告，不能声称显式交互建模。
- 当前推进中的 NTU 双人 ReGenNet 风格 diffusion：`window_len=60, obs_len=10, pred_len=50`，双人 raw axis-angle 转 canonical rot6d，条件为 obs10 + 26 类动作标签，目标为 future50，训练目标为 `L_dm + L_inter`。协议和实现见 `docs/ai/context/20260723-214702-ntu-two-person-forecasting-diffusion-regennet-design.md` 与 `docs/ai/context/20260723-215149-ntu-two-person-forecasting-diffusion-regennet-implementation-plan.md`。
- `L_dm-only` 不再是进入 `L_dm + L_inter` 的科学前置 gate；两者必须实际训练后比较。修订协议及 `L_dm + 1.0 * L_inter` CUDA 5000-step 完整训练结果见 `docs/ai/context/20260822-100014-ntu2p-diffusion-protocol-revision-plan.md` 与 `docs/ai/context/20260822-102600-ntu2p-diffusion-ldm-inter-full-training-result.md`。本轮 val 的五个 checkpoint 在 `xyz_mse/xyz_mae/mpjpe` 均未超过 `copy-last`。
- NTU 双人 diffusion 训练必须显式使用 `--device cuda:0`；禁止 CUDA 不可用时静默回退 CPU。设备策略见 `docs/ai/context/20260821-221534-ntu2p-diffusion-cuda-training-enforcement-result.md`。
- 独立单人 baseline 已用 `cuda:0` 完成 5000 step；设计、训练和 test 结果见 `docs/ai/context/20260822-102337-ntu2p-independent-single-person-baseline-result.md`。
- 当前新的主要比较 gate 是 `L_dm + L_inter` 双人联合模型同时超过 independent single-person xyz baseline 的 `xyz_mse/xyz_mae/mpjpe`；`copy-last` 继续作为非学习型 persistence 参考，direct joint xyz 作为联合输入学习型 reference。旧 diffusion 未过 gate 的深度排查见 `docs/ai/context/20260822-115553-ntu2p-diffusion-vs-independent-baseline-deep-audit.md`：问题集中在纯噪声自由采样与训练目标不对齐、首帧连续性未约束、`L_inter` 尺度过强及 rot6d 回归误差，不能归因于双人关系信息无效。
- NTU 双人显式跨人 attention diffusion 已实现并完成 CUDA `5000 step` 训练：共享单人投影、人物内 temporal self-attention、双向 cross-attention、24-token memory 和分人物 future decoder。实现、训练计划与结果见 `docs/ai/context/20260822-193000-ntu2p-explicit-cross-person-attention-training-plan.md`、`docs/ai/context/20260822-193000-ntu2p-explicit-cross-person-attention-training-result.md`；当前五个 val checkpoint 的 `DDIM50` paired xyz 主指标仍未超过 `copy-last`，尚不能声称 attention 带来性能提升。
- NTU 双人 baseline residual-refinement 已完成 CUDA `5000 step`：冻结 independent single-person xyz baseline，新增双流 temporal self-attention、双向 cross-person attention 和零初始化 residual head。1000/2000/3000/4000/5000 五个 val checkpoint 均同时超过 inherited baseline 与 `copy-last` 的 paired `xyz_mse/xyz_mae/mpjpe`，首帧误差保持 0。结果见 `docs/ai/context/20260822-214340-ntu2p-baseline-residual-refinement-training-result.md`；`inter_loss_weight` 消融计划见 `docs/ai/context/20260822-214340-ntu2p-residual-inter-loss-ablation-plan.md`。
- NTU 双人 residual refinement 的 `inter_loss_weight=0.01/0.05/0.1` 消融已完成。`0.01` 的最终 5000 step 最好，但没有在所有中间 checkpoint 稳定超过 `lambda=0`；`0.05/0.1` 中后期主指标退化。下一阶段默认保留 `inter_loss_weight=0`，先迁移到 residual diffusion；完整结果见 `docs/ai/context/20260822-222701-ntu2p-residual-inter-loss-ablation-result.md`。
- 当前最佳 residual refiner 的架构图、可复用 xyz 导出脚本和 8 个最佳案例视频已生成；架构与指标记录见 `docs/ai/context/20260823-084520-ntu2p-residual-refiner-architecture-visualization-result.md`。
- 该最佳 checkpoint 虽然 `xyz_mse/xyz_mae/mpjpe` 全面超过 base 与 copy-last，但视频显示人物骨架整体刚性平移、四肢基本不摆动。定量分析证实这是确定性 L2 回归对多模态未来姿态"回归到均值"：局部姿态（关节相对 root）帧间摆动能量仅为 GT 的 0.55%，而 root 帧间位移能量为 GT 的 5.8%（相差约一个数量级），且 37.6% 的关节-帧摆动幅度低于 GT 位移中位数的 10%（GT 自身仅 9.8%）。`xyz_mse/xyz_mae/mpjpe` 对该问题不敏感，不能仅凭这三个指标判断视觉质量。诊断与定量结果见 `docs/ai/context/20260902-143842-ntu2p-residual-refiner-regression-to-mean-diagnosis.md` 与 `docs/ai/context/20260902-144108-ntu2p-residual-refiner-regression-to-mean-quantitative-result.md`。
- 补充分解：学习型模型的 L2 收益主要来自 root（root MSE 相对 copy-last −44%，local −24%）；训练中 velocity/acceleration 项数值上仅占位置 MSE 的 0.6%，实际不起作用；模型输出是幅度约 1/3 的缓慢单调形变（时间 std 比值 0.34）而无振荡（帧差能量比值 0.0055），评估必须同时报告帧差能量口径。摆动恢复的调整设计（评估 gate 加入摆动指标、loss 尺度归一化、局部姿态速度损失、相位无关摆动能量匹配损失、饱和 ramp、正弦位置编码、双头输出、延长 step、Track B residual diffusion 的 mean-of-K 评估协议）与分阶段训练计划见 `docs/ai/context/20260902-145142-ntu2p-articulation-recovery-design.md` 与 `docs/ai/context/20260902-145142-ntu2p-articulation-recovery-training-plan.md`；用户已确认：mpjpe 相对 base 回退上限 5%、本轮 `inter_loss_weight=0.01`。
- 摆动恢复 Stage 0 已完成：新增摆动指标 `compute_ntu_articulation_metrics` 与评估 gate（`passes_full_gate` = L2 三项低于 copy-last + 能量比值 ≥ 0.10 + frozen ≤ 0.20 + mpjpe 相对 base 回退 ≤ 5%）、训练新选项（`--loss_scale_normalize`、`--local_velocity_loss_weight`、`--articulation_energy_loss_weight`、`--ramp_mode saturate`、`--future_pos_mode sinusoidal`，默认全部关闭）、随机选例导出与 Stage 1 串行驱动 `scripts/run_ntu2p_articulation_stage1.py`。默认参数下与 inter001 checkpoint 逐位等价（差异 ≤ 1.2e-7）。结果见 `docs/ai/context/20260902-150402-ntu2p-articulation-recovery-stage0-result.md`；Stage 1 汇总表在 `results/forecasting/ntu120_label/ntu2p_articulation_stage1/summary.md`。
- 摆动恢复 Stage 1 已完成：7 个单因子（去正则、20000 step、饱和 ramp、正弦位置编码、尺度归一化、局部速度损失、帧差能量匹配）全部未过摆动 gate；H1/H2/H3b 否定，饱和 ramp 弱有效，局部速度损失给出主线最佳 L2（mpjpe 0.2126，−6.1% vs base）。两个关键诊断：(a) 能量型损失在 Δ≈0 处梯度消失（梯度范数为 mse 的 0.34，开根号幅度型为 8.8），root 项梯度约为 mse 的 10 倍；(b) GT 帧差能量约 2/3 为拟合抖动，位置域 DCT 分频带才是公平口径——模型在 ≤1 Hz 有 GT 的 17%，在 1.2–2 Hz 步态频段仅 0.15%。指标已改为 `dct_low/mid/high_energy_ratio`，gate 为 `dct_low ≥ 0.40 且 dct_mid ≥ 0.10`。结果与 Stage 2 设计见 `docs/ai/context/20260902-173335-ntu2p-articulation-recovery-stage1-result.md`、`docs/ai/context/20260902-173335-ntu2p-articulation-recovery-stage2-design-and-plan.md`；Stage 2 汇总在 `results/forecasting/ntu120_label/ntu2p_articulation_stage2/summary.md`。
- **摆动恢复 Stage 2 已解决"整体平移、四肢不动"问题**：底座（`--loss_scale_normalize --local_velocity_loss_weight 1.0 --ramp_mode saturate`）+ 分频带幅度损失 `--dct_low_amplitude_loss_weight 1.0 --dct_mid_amplitude_loss_weight 1.0` + `--root_loss_weight 0.1`（run `s2_5_dct1_root01`）：mpjpe 0.1901（相对 base −16.1%、copy-last −31.6%），1.2–2 Hz 频带能量从 GT 的 0.15% → 57.8%，frozen 0.358 → 0.096（GT 0.098），高频带 < 0.01 无抖动，短/中/长 horizon 与 root/local 同时改善。机制修正：control 的残差在时间上严格线性（零初始化 future_pos 导致的鞍点，非条件均值），幅度型损失在鞍点处梯度不消失，推出后 L2 随之下降；`temporal_std` 只放大慢速漂移，`dct_mid_amplitude` 是关键项。5000 step 未收敛。结果见 `docs/ai/context/20260902-192249-ntu2p-articulation-recovery-stage2-result.md`；轨迹对比图 `results/forecasting/ntu120_label/ntu2p_articulation_stage2/joint_traj_random_cases_control_vs_s2_5.png`；Stage 3（seed 1/2 复现 + 20000 step）驱动 `scripts/run_ntu2p_articulation_stage3.py`，汇总 `results/forecasting/ntu120_label/ntu2p_articulation_stage3/summary.md`。
- 摆动恢复 Stage 3 已完成：s2_5 配置 3 seed @5000 mpjpe 0.1911 ± 0.0040、dct_mid 0.59 ± 0.10，全部过 gate；20000 step 摆动指标保持（其单 seed 数字不作主数字引用）。误差随 horizon 分解：root 误差近似线性增长不饱和（2.5 s 时 0.179，为长程偏差主因；"可修"假设经 root 轨迹 DCT 头与 inter 权重重测检验未获支持，见下方 rootdct 条目）；局部姿态误差 1.0–1.5 s 后饱和于 0.15–0.19（本质多模态，不可由确定性模型修复，交 Track B residual diffusion 并对 ≥1.5 s 改报合理性指标）；不要用加大 long/final 权重压长程 L2。结果与 horizon 曲线见 `docs/ai/context/20260902-205844-ntu2p-articulation-recovery-stage3-result.md`。
- NTU2P residual refiner 训练步数预算（据 20k 长训平台统计：8k–20k mpjpe 0.1823 ± 0.0023，train/val gap 扩大，膝点 6000–8000）：**消融/筛选 5000 step，最终数字 10000 step × 3 seed，取终点 checkpoint 报均值 ± 标准差，不在 val 上挑最好**；不再使用 20000。root/inter 与 v2 筛选已改用 10000 step（5000 step 的 seed 噪声约为 0.8% 采纳阈值的 3 倍，配对不降噪）。训练时加 `--ema_decay 0.999`，报告 `ema/` 下的终点权重（见下方 EMA 结论）。设计见 `docs/ai/context/20260902-212338-ntu2p-training-step-budget-design.md`。
- NTU2P 首次 test 评估（s2_5 × 3 seed × 10000 step 终点，原始权重；主数字已被下方 EMA 结果取代）：test（1253）0.1967 ± 0.0015，三项 L2 与摆动 gate 在 val/test 上 3/3；摆动恢复前的 control 在 test 上不过 gate（dct_mid 0.0017）。训练逐位确定，方差只来自 seed。计划、结果与 test horizon 曲线见 `docs/ai/context/20260924-113322-ntu2p-final-10k-3seed-test-eval-plan.md`、`docs/ai/context/20260924-130524-ntu2p-final-10k-3seed-test-result.md`。
- NTU2P 权重 EMA 结论（主数字已被下方 rootdct 取代，EMA 协议保留）：学习率余弦尾部衰减 × 权重 EMA 的 2×2 对比已完成，按预登记规则（只看 val）采纳 const-EMA，即恒定学习率 + `--ema_decay 0.999`、报告 `ema/` 终点。终点抖动 J 从 0.0031 降到 0.0004（−87%）；val mpjpe 0.1751 ± 0.0014；test（仅评估选中变体）mpjpe **0.1882 ± 0.0005**，xyz_mse/xyz_mae 同步改善，摆动 gate 3/3，dct_mid 不降反升。余弦尾部衰减单独使用未达标（J 仅 −33%），叠加 EMA 也不如只用 EMA，不采纳，开关保留、默认关闭。两个开关关闭时及开启 EMA 时原始权重均与已有 run 逐位相同（`torch.equal`）。EMA 对照 checkpoint 为 `save/forecasting/ntu120_label/ntu2p_residual_refiner_xyz_artic_ema_s2_5_dct1_root01_s{0,1,2}_10000/ema/model000010000.pt`。计划、实现、审查跟进与结果见 `docs/ai/context/20260924-154319-ntu2p-lr-cosine-ema-plan.md`、`docs/ai/context/20260924-155201-ntu2p-lr-cosine-ema-implementation-result.md`、`docs/ai/context/20260924-155510-ntu2p-lr-ema-driver-review-followup.md`、`docs/ai/context/20260924-190149-ntu2p-lr-cosine-ema-result.md`（其结论 6 的措辞更正见 `docs/ai/context/20260924-190412-ntu2p-lr-ema-result-review-followup.md`）。
- NTU2P root 轨迹 DCT 头结论（rootdct，旧 refiner 系最佳；主数字已被下方 v2 取代）：Stage A 按预登记规则（只看 val）只采纳 `--root_head_mode dct --root_dct_k 5`（s2_5 + EMA，10000 step × 3 seed，报告 `ema/` 终点）。val mpjpe 0.1751 ± 0.0014 → **0.1706 ± 0.0002**（−2.6%，xyz_mse −8.4%，xyz_mae −2.4%，gate 3/3）；test（仅评估选中配置）0.1882 ± 0.0005 → **0.1845 ± 0.0001**（−1.9%，相对 base −23.0%、copy-last −37.7%）。改善在整体平移与两人相对位置（test root −2.8%、相对 root −5.1%，局部姿态持平），第 15–40 帧最大；第 50 帧 root 平移误差回到对照水平，root 误差随 horizon 的整体斜率不变。Stage 3 "root 线性增长可修"的假设未获支持；长程漂移可能主要是内在不确定性，属未验证解读，交 Track B。inter 权重 1/3/10 均不采纳：10 使 L2 三项全部变差；3/1 的 L2 持平，关键关节误差稳定改善但接触误差变差。`inter_loss_weight` 保持 0.01（归一化下实际等于无 inter 项），不能据此声称交互建模收益。rootdct checkpoint 为 `save/forecasting/ntu120_label/ntu2p_residual_refiner_xyz_artic_rootdct_s2_5_dct1_root01_s{0,1,2}_10000/ema/model000010000.pt`。计划、实现与结果见 `docs/ai/context/20260925-110531-ntu2p-root-dct-head-inter-weight-plan.md`、`docs/ai/context/20260925-111713-ntu2p-root-dct-head-inter-weight-implementation-result.md`、`docs/ai/context/20260925-181731-ntu2p-root-dct-head-inter-weight-result.md`。
- NTU2P v2 端到端架构（用户 2026-09-26 确认采纳；主数字已由下条 +FD2 更新）：场景规范系双人 DCT-Mixer（InterMixer，取代冻结单人 base + 残差 refiner）+ 双人重合时规范系回退相机 x 轴 + 脚接触损失 0.05 + 可微骨架投影（骨长恒定、刚体手，对应 ReGenNet 的 FK 合法人体）+ 腿部专用流与相位敏感腿损失，配置 `A6-F-A5f0.05-A4-GH0.5`，3.65M 参数，10000 step × 3 seed，EMA 终点。test mpjpe **0.1810 ± 0.0004**，相对 rootdct 0.1846 为 −1.9%、相对旧 const-EMA 结构 0.1882 为 −3.85%，逐 seed 全负；xyz_mse/xyz_mae 同降，gate 3/3。test 自然度（相对 rootdct）：骨长误差 5.7% → 0，步行 GT 站定帧脚速 −40%，迈步相位相关 0.12 → 0.48，步数 0.34 → 1.34（GT 2.19），滑行帧比 +3%。关键诊断：55 关节中手指占 loss 81%、腿仅 7.7%，腿梯度又主要来自与相位无关的 DCT 中频幅度项，这是"腿不迈步、整体滑行"的根因；直接输出 xyz 时步行腿骨个别帧被拉长近 2 倍，而平均骨长误差（约 5%）掩盖了这一点。偏离：步态规则的"滑行帧比 ≤ 1.10× 参照"护栏未过（参照腿几乎不动、弓步前滑），采纳依据为审查图与多数自然度指标。val 收益（−7.8%）约为 test 两倍：val 受试者全在 train 中，选型应另设留出受试者。新数据管线（完整序列 xyz 缓存，GPU 常驻采样，3.6 倍速）、自然度与步态指标、审查图工具可复用。主线 checkpoint 为 `save/forecasting/ntu120_label/ntu2p_v2_A6-F-A5f0.05-A4-GH0.5_s{0,1,2}_10000/ema/model000010000.pt`（单权重可视化用 s0）；汇报数字用此组（替换 rootdct 的 0.1845 与 const-EMA 的 0.1882）。后续实验以 v2 为对照与底座（`train/train_ntu2p_v2.py`、`eval/eval_ntu2p_v2.py`、`scripts/run_ntu2p_v2_screen.py`），新方案须同时报告自然度/步态指标并看审查图。结果与过程见 `docs/ai/context/20260926-005610-ntu2p-v2-final-result.md`（其中引用各阶段文档），采纳决定见 `docs/ai/context/20260926-100701-ntu2p-v2-mainline-adoption-decision.md`；下一步候选：Track B residual diffusion（以 v2 终点为确定性底座）、手指损失去重、从 train 留出受试者作选型 val。
- **NTU2P 当前主线与主数字（v2 + 手指损失去重 FD2）**：配置 `A6-F-A5f0.05-A4-GH0.5-FD2`，即 s2_5 主损失只用身体 25 关节加每只手 index1/pinky1（`--loss_joint_subset hand2`），刚体手下 30 个手指关节原占 loss 81%。test mpjpe **0.1805 ± 0.0005**（v2 0.1810，逐 seed −0.10/−0.76/+0.10%），xyz_mse 0.02774，xyz_mae 0.08749，gate 3/3；test 滑行帧比 0.249 → 0.203（−18%，已低于 A0 0.214 与 rootdct 0.242），步数 1.34 → 1.46（GT 2.19），骨长误差 0。主线 checkpoint 为 `save/forecasting/ntu120_label/ntu2p_v2_A6-F-A5f0.05-A4-GH0.5-FD2_s{0,1,2}_10000/ema/model000010000.pt`（单权重可视化用 s0）。**选型协议改为受试者留出 val**：原 val 的受试者全部在 train 中，高估 v2 收益约 2 倍（val −7.8% vs test −3.85%）。新划分按预登记搜索留出 15 个受试者，得 train 1709 / val 247，NTU60 占比 0.547，test 为 0.555。同一批模型在新 val 上的 Δmpjpe 为 −3.0%，test 为 −4.4%，估计误差从约 4 个百分点降到约 1.4 个百分点，且偏保守。新协议下训练 run 前缀 `ntu2p_v2subj`，驱动参数 `--manifest_path results/forecasting/ntu120_label/ntu2p_subjval/manifest_subjval_seed0.json --cache_dir results/forecasting/ntu120_label/ntu2p_xyz_seq_cache_subjval --baseline_checkpoint save/forecasting/ntu120_label/ntu2p_independent_single_person_o10_p50_subjval_s0_5000/model000005000.pt --run_prefix ntu2p_v2subj`；采纳后再在原协议上训练 3 seed，只评估一次 test。等待后台进程请用 PID 或完成标记，不要用 `pgrep -f` 模式，曾因此死锁 3.5 h。见 `docs/ai/context/20260926-105152-ntu2p-subject-holdout-val-plan.md`、`docs/ai/context/20260926-162912-ntu2p-subject-holdout-val-result.md`、`docs/ai/context/20260926-165932-ntu2p-finger-dedup-result.md`。Track B 残差扩散（ResDiff-OOF+：以 v2+FD2 为冻结底座，对交叉拟合 OOF 残差的 DCT 系数做 x0 预测余弦扩散）已完成，按预登记规则**不采纳**（`reject_no_better_than_bootstrap`）：概率质量 ES 不如直接抽取真实残差的 bootstrap；步数分布和先迈脚更好（Brier 0.46 vs v2 1.11），但单样本 jerk、穿地、自身脚滑变差，审查图显示起步突跳与弓步；单步条件均值 L2 −2% 属均值估计（同为均值估计的 v2 三 seed 输出平均 L2 −3.3%，但 dct_mid 0.63→0.24），不作确定性改进。主线不变。见 `docs/ai/context/20260926-121543-ntu2p-trackb-residual-generative-design-and-plan.md`、`docs/ai/context/20260926-172000-ntu2p-trackb-review-fixes-and-nfe-preregistration-amendment.md`、`docs/ai/context/20260926-192241-ntu2p-trackb-residual-diffusion-result.md`。

## 解释边界

- InterHuman 本地 actor/reactor 顺序标签不是动作语义类别，不能当作“握手”等动作监督。
- 不声称 multi-person forecasting、interaction-aware 或 explicit relation 首创；relation-aware 只按已有同口径实验的相对收益表述。
- 历史 CMDM、旧 NTU120 长窗口和单 skeleton 视频记录已归档，不作为当前协议或结论；压缩前入口全文见 `docs/ai/context/20260822-100457-agents-content-archive.md`。
- 20260822 已按用户授权清理 `save/**/*.pt` 权重与 checkpoint，保留配置、指标、日志和诊断文件；执行记录见 `docs/ai/context/20260822-205033-save-weight-cleanup-plan.md`。

## 文档入口

- 长期项目记忆：`docs/ai/context/20260614-185753-regennet-key-context-extracted-from-agents.md`
- 阶段性设计、实验和结果：`docs/ai/context/` 下按时间戳文件
- 本次压缩计划与结果：`docs/ai/context/20260822-100457-agents-slimming-plan.md`、`docs/ai/context/20260822-100457-agents-slimming-result.md`
- 20260902 摆动恢复工作的文档索引、CLAUDE.md 建立、PR #1 合并与仓库同步记录：`docs/ai/context/20260902-221812-claude-md-setup-and-articulation-recovery-pr-sync-result.md`
- CLAUDE.md 软链接、本机 Claude 审查 + PR 自动合并 CI、本地 main 同步脚本：设计 `docs/ai/context/20260924-104641-claude-md-symlink-and-pr-auto-merge-design-and-plan.md`，实现与验证 `docs/ai/context/20260924-105223-claude-md-symlink-and-pr-auto-merge-result.md`，端到端结果 `docs/ai/context/20260924-105858-pr-auto-merge-pipeline-e2e-result.md`
- 每日文档维护（本机 crontab 每天 03:30 运行 `scripts/daily_docs_maintenance.py`：独立 worktree 里先清理 `docs/ai/context/`、再用本机 Claude Code 压缩本文件并把移除内容完整归档，经 `ship_pr.py` 自动合并；日志 `~/.local/state/regennet-docs-maintenance/`）：设计 `docs/ai/context/20260924-110849-daily-docs-maintenance-cron-design-and-plan.md`，实现与验证 `docs/ai/context/20260924-111530-daily-docs-maintenance-cron-result.md`，审查跟进（CI 校验被删文件已过保留期）`docs/ai/context/20260924-111820-daily-docs-maintenance-review-followup.md`，首次正式运行（PR #7，删 111、精简 1 条）`docs/ai/context/20260924-112805-daily-docs-maintenance-first-run-result.md`
